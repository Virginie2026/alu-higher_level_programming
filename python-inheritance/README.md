# Python - Inheritance

Exercises exploring Object-Oriented Programming inheritance in Python:
superclasses, subclasses, method overriding, multiple inheritance, and
built-in functions like `isinstance`, `issubclass`, `type`, and `super`.

## Tasks

### 0. Lookup
Write a function that returns the list of available attributes and methods
of an object.
