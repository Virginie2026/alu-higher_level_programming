# Python - Almost a circle

A project building a small class hierarchy (Base, Rectangle, Square) with
full unit test coverage, following PEP 8 style guidelines. Every class
and method is tested using Python's unittest framework, with all tests
located in the `tests/` directory and runnable via:

    python3 -m unittest discover tests
